# 腾讯云域名配置指南

## 📋 概述
本指南介绍如何在腾讯云上为法律合规审查系统配置域名和SSL证书，实现通过域名安全访问您的应用。

## 🎯 前提条件
1. 已购买腾讯云服务器（CVM）并完成应用部署
2. 已注册域名（建议使用`.com`、`.cn`等主流后缀）
3. 域名已完成备案（国内服务器必须备案）

## 🚀 域名解析配置

### 步骤1：登录腾讯云控制台
1. 访问 [腾讯云控制台](https://console.cloud.tencent.com)
2. 在顶部搜索栏搜索"域名注册"或"DNSPod"
3. 进入域名管理页面

### 步骤2：添加域名解析记录
1. 在域名列表中找到您的域名，点击"解析"
2. 点击"添加记录"，配置如下：

#### A记录（推荐）
| 参数 | 值 | 说明 |
|------|-----|------|
| 主机记录 | @ 或 www | @表示根域名，www表示www子域名 |
| 记录类型 | A | IPv4地址记录 |
| 线路类型 | 默认 | 默认线路 |
| 记录值 | 您的服务器公网IP | 从CVM控制台获取 |
| TTL | 600 | 10分钟缓存时间 |

#### CNAME记录（如果需要子域名）
| 参数 | 值 | 说明 |
|------|-----|------|
| 主机记录 | legal-audit | 子域名前缀 |
| 记录类型 | CNAME | 别名记录 |
| 线路类型 | 默认 | 默认线路 |
| 记录值 | legal-audit.yourdomain.com | 指向主域名 |
| TTL | 600 | 10分钟缓存时间 |

### 步骤3：验证解析生效
```bash
# 使用nslookup验证解析
nslookup yourdomain.com
nslookup www.yourdomain.com

# 使用dig验证（如果可用）
dig yourdomain.com A
```

等待DNS生效（通常5-30分钟），然后进行下一步。

## 🔐 SSL证书配置

### 方案一：使用腾讯云SSL证书（推荐）

#### 步骤1：申请免费证书
1. 登录腾讯云控制台，搜索"SSL证书"
2. 点击"申请免费证书"
3. 选择"域名型免费证书（DV）"
4. 填写域名信息：
   - 证书品牌：TrustAsia
   - 证书类型：免费版
   - 域名：填写您的域名（支持通配符）
5. 点击"下一步"，选择自动DNS验证
6. 提交申请，等待审核（通常几分钟到几小时）

#### 步骤2：下载证书文件
1. 证书签发后，在证书列表中找到您的证书
2. 点击"下载"，选择"Nginx"格式
3. 解压下载的ZIP文件，包含以下文件：
   - `yourdomain.com_bundle.crt` - 证书文件
   - `yourdomain.com.key` - 私钥文件

#### 步骤3：上传证书到服务器
```bash
# 在服务器上创建SSL证书目录
mkdir -p /opt/legal-audit/nginx/ssl

# 上传证书文件（使用scp或SFTP工具）
# 本地执行：
scp yourdomain.com_bundle.crt root@你的服务器IP:/opt/legal-audit/nginx/ssl/
scp yourdomain.com.key root@你的服务器IP:/opt/legal-audit/nginx/ssl/

# 设置正确的权限
ssh root@你的服务器IP "chmod 644 /opt/legal-audit/nginx/ssl/*"
```

#### 步骤4：配置Nginx使用SSL证书
修改 `/opt/legal-audit/nginx/conf.d/legal-audit.conf`：

```nginx
# 更新SSL证书路径
ssl_certificate /etc/nginx/ssl/yourdomain.com_bundle.crt;
ssl_certificate_key /etc/nginx/ssl/yourdomain.com.key;
```

#### 步骤5：重启Nginx服务
```bash
# 重启Docker容器
cd /opt/legal-audit
docker-compose restart nginx

# 或重新构建（如果需要）
docker-compose down
docker-compose up -d
```

### 方案二：使用Let's Encrypt免费证书

#### 步骤1：安装Certbot
```bash
# 连接到服务器
ssh root@你的服务器IP

# 安装Certbot和Nginx插件
apt update
apt install certbot python3-certbot-nginx -y
```

#### 步骤2：获取证书
```bash
# 获取SSL证书（自动配置Nginx）
certbot --nginx -d yourdomain.com -d www.yourdomain.com

# 如果使用Docker，可能需要手动配置
certbot certonly --nginx \
  -d yourdomain.com \
  -d www.yourdomain.com \
  --email your-email@example.com \
  --agree-tos \
  --no-eff-email
```

#### 步骤3：配置证书路径
Certbot会自动创建证书文件在：
- 证书文件: `/etc/letsencrypt/live/yourdomain.com/fullchain.pem`
- 私钥文件: `/etc/letsencrypt/live/yourdomain.com/privkey.pem`

#### 步骤4：配置Nginx使用Let's Encrypt证书
修改Nginx配置：
```nginx
ssl_certificate /etc/letsencrypt/live/yourdomain.com/fullchain.pem;
ssl_certificate_key /etc/letsencrypt/live/yourdomain.com/privkey.pem;
```

#### 步骤5：设置自动续期
```bash
# 测试续期
certbot renew --dry-run

# 设置定时任务（每天自动检查续期）
crontab -e
# 添加以下行
0 12 * * * /usr/bin/certbot renew --quiet
```

## 🔧 Nginx高级配置

### 强制HTTPS重定向
确保所有HTTP请求都重定向到HTTPS：

```nginx
server {
    listen 80;
    server_name yourdomain.com www.yourdomain.com;
    return 301 https://$server_name$request_uri;
}
```

### 性能优化配置
```nginx
# 在http块中添加
gzip on;
gzip_vary on;
gzip_comp_level 6;
gzip_types text/plain text/css text/xml text/javascript application/json application/javascript application/xml+rss image/svg+xml;

# 静态资源缓存
location ~* \.(jpg|jpeg|png|gif|ico|css|js)$ {
    expires 1y;
    add_header Cache-Control "public, immutable";
}
```

### 安全头配置
```nginx
add_header X-Frame-Options "SAMEORIGIN" always;
add_header X-Content-Type-Options "nosniff" always;
add_header X-XSS-Protection "1; mode=block" always;
add_header Strict-Transport-Security "max-age=63072000; includeSubDomains; preload" always;
add_header Referrer-Policy "strict-origin-when-cross-origin" always;
```

## 📊 验证配置

### 测试SSL证书
```bash
# 使用openssl测试证书
openssl s_client -connect yourdomain.com:443 -servername yourdomain.com

# 使用SSL Labs测试（在线工具）
# 访问：https://www.ssllabs.com/ssltest/
```

### 测试网站访问
```bash
# 测试HTTP重定向
curl -I http://yourdomain.com
# 应该返回 301 重定向到HTTPS

# 测试HTTPS访问
curl -I https://yourdomain.com
# 应该返回 200 OK
```

### 测试API端点
```bash
# 测试健康检查
curl https://yourdomain.com/api/performance?action=healthCheck

# 测试文件上传
curl -X POST https://yourdomain.com/api/upload \
  -F "file=@test.pdf"
```

## 🛡️ 安全配置建议

### 1. 防火墙配置
